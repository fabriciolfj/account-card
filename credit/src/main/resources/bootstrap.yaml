spring:
  application:
    name: account-card-credit
  cloud:
    config:
      uri:
        - http://localhost:8888
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true


